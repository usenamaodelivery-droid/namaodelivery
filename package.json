{
  "name": "namao-delivery",
  "version": "1.5.5",
  "description": "Delivery NaMão — Capacitor Android app (driver-only) + Firebase backend",
  "private": true,
  "scripts": {
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "android:build:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:build:release": "cap sync android && cd android && ./gradlew assembleRelease",
    "deploy:firebase": "firebase deploy --only firestore:rules,storage,functions"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.26",
    "@capacitor/android": "^8.3.1",
    "@capacitor/camera": "^8.1.0",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/geolocation": "^8.2.0",
    "@capacitor/push-notifications": "^8.0.3",
    "firebase": "^12.12.1"
  }
}
